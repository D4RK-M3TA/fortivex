# FortiVex Site — run production build locally
# Usage: docker compose up --build   then open http://localhost:8080

services:
  web:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: unless-stopped
